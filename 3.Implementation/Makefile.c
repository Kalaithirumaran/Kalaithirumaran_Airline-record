#target  : Dependencies
#<TAB>commands

Build : project_main.c airline record.c test_airline record. unity.c
        gcc project_main,c airline record.c test_airline record.c unity.c -0 airline record.out
Run : airline record.out
            ./airline record.out
Clean :
       rm -rf *.0 *.out *.i *.s      
